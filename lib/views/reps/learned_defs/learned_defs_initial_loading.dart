import 'package:flutter/material.dart';

class LearnedDefsInitialLoading extends StatelessWidget {
  const LearnedDefsInitialLoading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}
