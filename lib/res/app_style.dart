import 'package:bag_of_words/res/app_color.dart';
import 'package:flutter/material.dart';

class AppStyle {
  static const brCircular16 = const BorderRadius.all(Radius.circular(16));

  // Text styles
  static const tsAppName = const TextStyle(
    fontSize: 22.0,
    fontWeight: FontWeight.bold,
    color: AppColor.wordColor,
  );
}
